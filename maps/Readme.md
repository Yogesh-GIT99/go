# Maps:
1. Introduction
2. Mutating maps
3. Map vs Structs: Maps are arrays where instead of indexes we are using labels to define our values. Struct is good in cases where we have fixed items defined where as maps solves a problem which requires to map values using labels and includes functionalities as deleting/adding and overwriting of data's. 